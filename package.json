{
  "name": "shiftsyncdemo",
  "version": "2.0.0",
  "description": "Playwright E2E tests for SauceDemo using Page Object Model (TypeScript)",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "typescript": "^5.4.0",
    "@types/node": "25.5.0"
  }
}
